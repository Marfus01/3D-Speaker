AVD的核心在于利用face track和active speaker face信息，修正语音聚类结果。由于修正是单向的，在将其作为baseline时，我们仅需要评估语音聚类的效果。
经典的AVD只涉及视觉、语音两个模态，从消融实验的原则出发，我们暂时不考虑对文本信息的利用。
> 同时，也是为了回应另外一个审稿人对于利用文本信息对实验效果影响的担忧。

AVD中一般不涉及根据伪标签微调模型的过程，因此我们也暂时不考虑这一部分。


## 不依赖文本中别名出现情况
在仅将HMM视为一种对聚类标签修正方式的情况下，无需评估EER，因为模型参数没有发生任何变化。
### 纯语音聚类
效果较差。获得多个聚类簇之后，在已知主要说话人数量=n的情况下，可以定义cost matrix，使用匈牙利算法匹配聚类簇与标注说话人，获得理想情况下最佳的accuracy。
> 正文里放最终聚出n 类的结果即可，过程放附录

### 语音+视觉联合聚类
在通过谱聚类实现 audio-only clustering，通过AHClustering实现 visual-only clustering 之后，
1. 筛选至少一个visual segment与某audio簇的重叠时长>1s 的visual簇（以及与其overlap的audio segment embedding 的均值作为聚类中心），
2. 各个 audio 簇，查找与其重叠时长>0.5s的 visual 簇，并计算前者中各个样本与后者中各个聚类中心的余弦相似度，据此将所有audio segment分配到与其最相似的visual簇上（如果没有任何visual簇与其重叠>0.5s，则保持其audio-only聚类结果不变）。
3. 将仅包含 1 个样本的 audio簇合并到大簇，然后做大簇间合并。
> 需要统计Active speaker 在多少帧能检测到，识别效果对不对
实际上同时完成了
1. 根据视觉聚类效果修正语音聚类结果；
2. 将语音聚类簇标签与人脸聚类簇标签对齐。
对于经过修正&对齐的语音聚类簇标签，仍然可以使用上述方式评估其acc。
> 和 https://arxiv.org/pdf/2007.01216 中构建数据集所用的自动化pipeline类似，但不完全相同。在3D-speaker 中作为引用 40 被提到。该文章中提到的方法优先按照active speaker face直接分配说话人身份，对于未分配的语音片段，计算它们与已分配的语音片段的相似度，根据最近邻进行分配。在我们的数据集中，高质量的active speaker face对台词覆盖率有限，这会导致已分配语音片段数量不足，难以形成高质量的聚类中心，影响未分配语音片段的分配效果。
> ** https://arxiv.org/pdf/2007.01216 ** 在rebuttal阶段先不实现，大修的时候再加。
> 问题 1：这两种方法选一种实现即可，还是都实现？

### 视觉约束的语音聚类
仅做了根据视觉聚类效果修正语音聚类结果这一步。
对于经过修正&对齐的语音聚类簇标签，仍然可以使用前述方式评估其acc。
> 在ACL2025中提出，在3D-Speaker-Toolki的paper中作为多模态SPEAKER DIARIZATION与现有方法比较。
> rebuttal 阶段先不实际实现，直接 report 语音+视觉联合聚类的结果即可。

### HMM
先试一下退化版本效果，不行的话把完整版本的viterbi部分修复，再测一下。
#### 退化版本
忽略视觉信息，直接将纯语音聚类簇/经过前两种方式修正的语音聚类簇作为HMM的observation，进行HMM修正。
#### 完整版本
将语音+视觉联合聚类结果作为HMM的observation，进行HMM修正。
> HMM要求observation中音频和视觉聚类伪标签是aligned的，因此需要先通过语音+视觉联合聚类对齐。相比原来直接将一个语音簇完全分配到一个视觉簇的方式，3d-speaker使用的对齐方式更加细粒度。
> 即使不将语音+视觉联合聚类作为baseline，至少可以作为初始状态展示。
> 问题 2：直接做完整版本，还是先做退化版本？


## 后续(暂时不做)
1. 利用文本中别名出现情况，建立align后的语音聚类簇标签到人名的映射。
2. 根据伪标签微调模型，评估EER。